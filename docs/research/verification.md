# R20260908 검증 기록

대상 실행 소스: `c61a74f66a1b1f2871c32d3c06e03c4e8486c2b5`. `src/autobit` 운영 코드는 기준 `f719dae`와 동일합니다. 연구 실행기·테스트·문서만 추가했습니다.

후속 도구 보완 `97d9489`는 코드·라이브러리 정보를 읽는 부분을 안전 규칙에 맞게 분리한 변경입니다. 전략 계산·실험 설정·기존 결과를 바꾸지 않았습니다. 당시 결과는 계속 `c61a74f`에 연결합니다.

## 확인된 실행

| 검사 | 결과 |
| --- | --- |
| 기존 오류 수정 관련 두 통합 테스트 파일 | 84 passed |
| 전략·공통 엔진·세 환경 계약·백테스트 실행·위험 시간·기준 golden과 초기 연구 테스트 | 95 passed |
| 출처·캐시 식별·부분 보고서·후보 적용·미래 구간 격리를 포함한 최종 연구 테스트 | 8 passed |
| 7년 전체 실험 | 3개 설정 × 2개 비용 = 6회 완료 |
| TRAIN/OOS 시간분리 실험 | 3개 설정 × 2개 비용 × 19개 구간 × 2단계 = 228회 완료 |
| 기존 전체 결과 재현 | 요약·거래·주문·자산곡선·품질의 5개 파일 바이트 일치 |
| 기존 기본 비용 시간분리 결과 재현 | 수익·최대 하락·거래 수 일치 |
| 재개 통합 검증 | `run_backtest` 호출을 오류로 바꾼 상태에서 234개 완료 결과를 모두 재사용하고 동일 비교·체크섬 확인 |
| 독립 수치·문서 검사 | 6개 성적 행, 후보 전체 설정, 38개 구간 행, 333개 거래, 527개 산출물 체크섬 일치 |
| 전체 테스트 최초 실행 | 1472 passed, 1 failed, 70 skipped / 2198.06초 |
| 실패 보완 후 전체 safety 디렉터리와 연구 테스트 | 122 passed / 63.10초 |
| 2026-09-08 사용자 요청으로 최신 PR 코드 전체 재검사 | **1486 passed, 0 failed, 70 skipped / 1047.48초 (17분 27초)** |

95개와 8개 집합에는 중복이 있으므로 합산해 고유 테스트 수로 표시하지 않습니다. 첫 84개 실행은 임시 디렉터리 상위 폴더가 없어 27개 setup 오류가 났고, 폴더 준비 후 동일 검사를 다시 실행해 84개 모두 통과했습니다. 결과를 로직 실패나 처음부터 통과한 기록으로 바꾸지 않습니다.

전체 검사에서 실패한 1개는 새 연구 도구의 `subprocess`·`importlib.metadata` 사용을 저장소 안전 규칙이 거부한 사례입니다. 고정된 Git 읽기 3개와 설치 버전 읽기 6개를 작은 전용 모듈로 분리하고, 그 파일 경로와 검토된 구문 구조가 정확히 같을 때만 허용했습니다. 일반 가져오기 금지와 실전 잠금 규칙은 유지했습니다. 명령 변경·shell 추가·네트워크 추가·문법 오류·다른 경로 복사를 거부하는 검사와 다른 체크아웃의 연구 모듈을 거부하는 검사를 추가했습니다. 독립 재리뷰에서 추가 지적은 없었습니다.

보완 직후에는 안전·연구 검사 122개를 실행했고, 이후 2026-09-08 사용자 요청으로 PR #3의 `8ee8f9ac3724c2a5b554de5c0f0e319071e442ed`에서 전체 1556개를 다시 실행했습니다. 결과는 1486 passed, 0 failed, 70 skipped입니다. 이전에 실패했던 저장소 안전 검사와 두 번의 실제 시간분리 계산 결과를 바이트 단위로 비교하는 장시간 검사도 통과했습니다. 이번 후속 변경은 검사 기록 문서만 갱신하며 검사한 코드와 테스트는 바꾸지 않습니다.

70개 skip은 실행 가능한 WSL 배포판이 없는 OCI 셸 테스트입니다. installer 37개, release transaction 21개, service verification 12개이며 통과로 계산하지 않습니다. 전체 검사 통과는 전략 수익 보장, 신규 후보 채택, main 병합 또는 서버 배포를 뜻하지 않습니다.

재검사 명령은 해당 연구 작업 폴더에서 아래와 같습니다. 임시 폴더는 기존 결과를 덮어쓰지 않도록 새 이름을 사용했습니다.

```powershell
$env:PYTHONPATH = (Resolve-Path 'src').Path + ';' + (Get-Location).Path
$env:PYTHONDONTWRITEBYTECODE = '1'
$env:PYTHONUNBUFFERED = '1'
python -m pytest -v -p no:cacheprovider --basetemp .test-tmp/full-20260908T012318Z
```

최종 로그는 저장소 루트 `reports/research-20260908/verification-full-20260908T012318Z.txt`입니다. 이전 로그 `verification-pytest.txt`, `verification-safety-final.txt`도 보존합니다. 위 검사 수는 서로 중복되므로 합산하지 않습니다.

## 독립 코드 리뷰

초기 도구 `e36b563`에서 확인된 Important 세 가지를 보완했습니다.

- 다른 체크아웃의 Python 패키지를 실행하면서 현재 코드를 보관할 가능성: 실제 모듈·폴더형 패키지 출처를 확인합니다.
- 다른 후보의 결과 파일과 체크섬을 함께 가져왔을 때 캐시 재사용 가능성: 실험·후보·비용·구간·단계·전체 설정을 결과에 묶습니다.
- 중단된 전체 보고서나 오래된 요약을 완료로 취급할 가능성: 완성된 보고서 폴더를 게시하고 재개 시 파일·식별값·요약을 확인합니다.

독립 OOS 파일에도 후보 ID를 넣었습니다. 회귀 테스트는 식별값 혼동·다른 출처·부분 결과·미래 값 변경에서 실패하는 사례를 확인한 뒤 수정했습니다. 정상 폴더형 패키지를 거부하던 추가 문제도 실패 테스트로 재현하고 수정했습니다. 최종 `c61a74f` 재리뷰와 결과 문서 리뷰에는 추가 actionable finding이 없었습니다. 리뷰어의 수치 검증과 테스트 실행 결과는 서로 구분합니다.

## 범위와 보존

- `.env`, VM, 서비스 설정, 운영 원장, 실전 잠금, 유료 자원은 변경하지 않았습니다.
- 연구 완료 당시 원격 작업은 하지 않았으며, 이후 2026-09-08 사용자 승인으로 코드·문서 업로드와 [오류 수정 PR #2](https://github.com/seungminbluebox/autoBit/pull/2), [연구 PR #3](https://github.com/seungminbluebox/autoBit/pull/3)를 생성했습니다. main 병합·배포는 실행하지 않았습니다. 오류 수정 내용은 [별도 기록](backtest-fix-pr.md)입니다.
- 연구 상세 결과와 원본·소스 사본은 로컬 보관입니다. 기존 보고서와 예비 실행은 보존했습니다.
- 위키는 별도 브랜치이고 현재 연구 브랜치에는 MkDocs 도구가 없습니다. 따라서 위키 전체 빌드를 통과했다고 주장하지 않습니다. 새 연구 Markdown의 내부 연결·공개 정보 패턴을 확인하고 루트 README에 연결합니다.
- 신규 후보의 DSR/PBO 및 새로운 미관측 시장 검증은 완료하지 않았습니다. 기존 공식 PASS 기준을 낮추지 않았습니다.

[연구 목록](README.md) · [결과 브리핑](R20260908-results.md)
